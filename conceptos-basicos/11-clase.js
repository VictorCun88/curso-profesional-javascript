// undefined, null y NaN

// * * *  undefined

/*
  undefined es una propiedad del objeto global, es decir, una variable de alcance global. El valor inicial de undefined es el valor primitivo undefined.this needs clarification, but that would require explaining primitive values
*/

typeof nombre;

// es una variable Indefinida 
// El resultado es : undefined


// * * * null

// El valor null es un literal de Javascript que representa intencionalmente un valor nulo o "vacío". Es uno de los valores primitivos de Javascript.

// nombre existe, pero no tiene tipo ni valor:

let nombre = null;

// * * * NaN

// La propiedad global NaN es un valor que representa Not-A-Number.